<h1>Tasks</h1>

<% @tasks.each do |task| %>
<%# make sure to change the link to the correct place %>
<li><%= link_to task.name, task_path(task) %></li><br>
<% end %>
<%= link_to '+ new task', tasks_new_path %>
